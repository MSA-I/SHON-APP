# שון בלאיש — אפליקציית תכנון אירועים

אפליקציית דסקטופ מקומית למעצב האירועים שון בלאיש — פגישות לקוחות, בחירת עיצובים, חתימה דיגיטלית וייצוא DOCX יוקרתי.

**Stack:** Tauri 2 · React 19 · TypeScript · Vite 7 · Tailwind v4 · IndexedDB · docx@8

> **Local-first.** אין שרת, אין ענן, אין התחברות. הכל נשמר במכונה של שון.
> **Hebrew RTL** מכל קצה לקצה.
> **תמונות לא מועתקות** — האפליקציה קוראת מהתיקיות הקיימות בלבד.

---

## דרישות מקדימות

| כלי | גרסה |
|---|---|
| Node.js | 20+ (מומלץ 22) |
| Rust + Cargo | latest stable (`winget install Rustlang.Rustup`) |
| Windows | 10/11 x64 |
| WebView2 | מותקן ברירת מחדל ב-Windows 11 |

---

## התקנה ראשונה

```bash
# 1. קלון לתיקייה הקבועה (עם תיקיות התמונות לידה)
git clone https://github.com/MSA-I/SHON-APP.git "F:/MyFiles/העסק שלי/שון בלאיש"
cd "F:/MyFiles/העסק שלי/שון בלאיש/app"

# 2. התקנת תלויות
npm install

# 3. בנייה ל-installer (.exe)
npm run tauri:build -- --bundles nsis
```

ה-installer יווצר ב:

```
app/src-tauri/target/release/bundle/nsis/Shon Blaish_<version>_x64-setup.exe
```

הרצת ה-installer פעם אחת — והאפליקציה זמינה כקיצור דרך רגיל.

---

## פיתוח (dev mode)

```bash
cd app
npm run tauri:dev    # פותח את האפליקציה עם hot reload
```

טסטים:

```bash
npm test             # vitest run (153 טסטים)
npm run typecheck    # tsc --noEmit
```

---

## מבנה התיקיות הצפוי

```
F:/MyFiles/העסק שלי/שון בלאיש/
├── app/                            # קוד האפליקציה (Tauri + React)
├── architecture/                   # SOPs (16 מסמכים)
├── assets/                         # לוגואים (SVG)
├── claude.md                       # חוקה: סכמות נתונים, חוקי התנהגות
├── task_plan.md                    # שלבי הפרויקט
├── progress.md                     # יומן התקדמות
├── findings.md                     # תובנות והחלטות עיצוב
│
├── events/                         # ⚙️ נוצר אוטומטית — DOCXים מיוצאים
├── backups/                        # ⚙️ נוצר אוטומטית — JSON גיבויים (30 אחרונים)
│
└── [תיקיות תמונות — לא בריפו, נמצאות מקומית]
    ├── אולם עיצוב בסיס 2026/
    ├── חופות אולם גדול גאמוס/
    ├── חופות ריזורט/
    ├── חופות שידרוג/
    ├── מפות מפיות/                  # ~520 תמונות
    ├── עיצובים שידרוג/
    ├── ריזורט בסיס/
    ├── כסא כלה בחוץ בסיס.jpg
    └── כסא כלה בתוך האולם.jpg
```

> **חשוב:** הריפו לא מכיל את התמונות. הן חיות מקומית בנפרד וצריכות להיות באותה תיקיית-אם של הפרויקט (`F:/MyFiles/העסק שלי/שון בלאיש/`).

---

## הרצה ראשונה (First Boot)

1. בהפעלה ראשונה תיפתח **ה-Tagging Pass** — הליכה חד-פעמית על כל התמונות לתיוג.
2. בסיום (`taggingComplete=true`) הדלת נסגרת לתמיד — מסך הבית הופך זמין.
3. גיבוי אוטומטי נוצר ב-`backups/` בכל חתימה / השלמת אירוע / סיום ה-Tagging Pass.

> **Skip button זמני:** יש כפתור "דלג זמנית" ב-Tagging Pass לצורכי תצוגה מקדימה. **חובה להסירו לפני שילוח לשון.**

---

## גיבוי ושחזור

| פעולה | מיקום |
|---|---|
| גיבוי ידני | Settings → "ייצוא גיבוי" |
| שחזור | Settings → "ייבוא גיבוי" |
| גיבויים אוטומטיים | `backups/backup_YYYY-MM-DD_HH-mm.json` (rolling 30) |
| Schema version | `BACKUP_SCHEMA_VERSION = 2` (תומך גם ב-v1 עם migration) |

---

## חוקי הפרויקט

ראה [`claude.md`](claude.md) לחוקה המלאה. הכי חשוב:

1. **אין רשת** — שום API חיצוני, שום הרשאות, שום ענן.
2. **תמונות נשארות במקום** — האפליקציה לא מעתיקה אותן.
3. **DOCX תמיד בהיר** — גם אם המשתמש בחר theme כהה ב-UI.
4. **אין מחירים** — שדרוגים הם טקסט תיאורי בלבד.
5. **אין mobile** — desktop only.

---

## רישוי

פרויקט פרטי עבור שון בלאיש הפקות.
