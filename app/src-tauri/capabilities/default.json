{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window. Filesystem and dialog scopes are added in capabilities/main.json by Task #8.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
